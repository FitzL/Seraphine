class ChatterBox{
    TimeInterval;
    Phrases;
    LastPhrase;
    constructor(TimeInterval, Phrases) {
        this.TimeInterval = TimeInterval;
        this.Phrases = Phrases;
        this.LastPhrase = Date.now();
    }

    getNextPhrase() {
        Math.random
    }

    getPhrases() {
        return this.Phrases;
    }

    getTimeInterval() {
        return this.getTimeInterval;
    }
}

module.exports.ChatterBox = ChatterBox;